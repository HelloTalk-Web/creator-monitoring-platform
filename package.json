{
  "name": "creator-monitoring-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "deploy": "cd frontend && npm install && npm run build && npx wrangler pages deploy out",
    "dev": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}